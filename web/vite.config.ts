import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  // The hub serves the built bundle itself, so there is only ever one origin to
  // expose through the tunnel and one URL to paste into Notion.
  build: {
    outDir: '../hub/gpu_hub/static',
    emptyOutDir: true,
  },
  server: {
    proxy: {
      '/api': 'http://127.0.0.1:8000',
    },
  },
})
